package Polymorphism;

//In method over loading concept we can write same method with same name but using different parameter.
//But we remember one thing that we also can change the data type name according to their size(smaller to bigger)

public class Overloading {

    public int m1(){
       return 0;
    }

    public double m1(int x) {
        return 0;
    }
}
